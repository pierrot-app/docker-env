version: '3'
services:
  node:
    tty: true
    build: 
      context: ./node
      args:
        NODE_ENV: ${NODE_ENV}
    container_name: 'service-polly'
    volumes:
      - ./node/express:/var/www/express
    ports:
      - '3000-30020:3000-30020'
    command: npm run ${NODE_ENV}
